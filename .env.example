# Lovable Cloud (Supabase) — publishable values, safe to expose in client bundle
VITE_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-publishable-key"
VITE_SUPABASE_PROJECT_ID="your-project-ref"

# Never commit real secrets here — use Lovable Cloud secrets for service-role keys,
# Resend keys, AI gateway keys, etc.
